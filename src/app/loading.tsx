export default function LoadingPage() {
    return (
        <main className="flex justify-center items-center h-screen">
            <section>
                <p>Loading...</p>
            </section>
        </main>
    );
}
