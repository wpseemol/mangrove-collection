import LogoutButton from '@/app/(public)/profile/_components/logout-button';

export default function DashboardPage() {
    return (
        <main>
            {' '}
            <h2>this is admin dashboard page</h2>
            <LogoutButton />
        </main>
    );
}
